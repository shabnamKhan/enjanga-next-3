/**
 * A "Skip Navigation Link" (often called "Skip to main content") plays a critical
 * accessibility role by allowing keyboard and screen reader users to bypass repeated
 * navigation menus and jump directly to the main content of the page:
 * - Enhances keyboard navigation
 * - Improves screen reader experience
 * - Meets WCAG Guidelines
 */
export { default as SkipNavigationLink } from './SkipNavigationLink';
